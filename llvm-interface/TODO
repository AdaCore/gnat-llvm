Missing main features (identified so far):

- extended return (see e.g. a-chahan.adb)
- multi-dimension arrays
- comparison of fat pointers (and detecting they're fat pointers)
- variant records only partly supported
- cleanups/at end handlers
- secondary stack support
- exception handlers
- expression renaming
- tagged types
- freeze nodes
- overflow checks (consider enabling the front-end mechanism?)
- representation clauses
- debug info
